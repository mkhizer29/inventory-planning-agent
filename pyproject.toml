[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "naheed-inventory-etl"
version = "0.1.0"
description = "Inventory Planning Agent — ETL (Layer 2): Magento pg_1 -> canonical warehouse"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["etl"]
