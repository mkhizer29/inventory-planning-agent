# Inventory Planning Agent — ETL (Layer 2) dependencies
# Pinned to versions known-good on Python 3.12.
pandas==2.2.3
numpy==2.1.3
SQLAlchemy==2.0.36
PyMySQL==1.1.1
PyYAML==6.0.2
python-dotenv==1.0.1
tabulate==0.9.0          # pretty DQ report tables
holidays==0.60          # Pakistan public-holiday calendar for external_signals
pytest==8.3.4           # tests

# --- Forecasting pilot (shared scorecard + the 3 models) ---
pyarrow==17.0.0          # read/write data/processed/*.parquet
statsmodels==0.14.4      # Holt / Holt-Winters exponential smoothing
lightgbm==4.5.0          # global gradient-boosted model
scikit-learn==1.5.2      # model utilities / metrics helpers

# --- Dashboard (Streamlit UI + Plotly charts; imported by dashboard/app.py) ---
streamlit==1.60.0        # dashboard UI
plotly==6.9.0            # interactive charts (incl. Phase B risk trajectory)
openpyxl==3.1.5          # in-memory .xlsx table exports (dashboard/export_utils.py)
reportlab==4.2.5         # in-memory .pdf table reports (dashboard/export_utils.py)
